package protocol

// PromptSchemaVersion is shared by Runtime and Gateway compaction metadata.
// Bump it whenever the byte-level prompt envelope changes.
const PromptSchemaVersion = "prompt-envelope-v1"
